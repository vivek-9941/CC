{
  "name": "task-mern-app",
  "version": "1.0.0",
  "description": "Unified runner for Task Lifecycle Manager",
  "scripts": {
    "start": "pm2 start backend/server.js --name task-api --watch",
    "stop": "pm2 stop task-api",
    "logs": "pm2 logs task-api",
    "status": "pm2 status",
    "dev": "npx concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
