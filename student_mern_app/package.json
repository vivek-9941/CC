{
  "name": "student-mern-app",
  "version": "1.0.0",
  "description": "Unified runner for Student Management Portal",
  "scripts": {
    "start": "pm2 start backend/server.js --name student-api --watch",
    "stop": "pm2 stop student-api",
    "logs": "pm2 logs student-api",
    "status": "pm2 status",
    "dev": "npx concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
