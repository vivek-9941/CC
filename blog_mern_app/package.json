{
  "name": "blog-mern-app",
  "version": "1.0.0",
  "description": "Unified runner for AURA Blog Platform",
  "scripts": {
    "start": "pm2 start backend/server.js --name blog-api --watch",
    "stop": "pm2 stop blog-api",
    "logs": "pm2 logs blog-api",
    "status": "pm2 status",
    "dev": "npx concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
