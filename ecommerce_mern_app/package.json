{
  "name": "ecommerce-mern-app",
  "version": "1.0.0",
  "description": "Unified runner for Nexus Ecommerce Store",
  "scripts": {
    "start": "pm2 start backend/server.js --name ecomm-api --watch",
    "stop": "pm2 stop ecomm-api",
    "logs": "pm2 logs ecomm-api",
    "status": "pm2 status",
    "dev": "npx concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
